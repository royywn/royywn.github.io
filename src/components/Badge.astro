---
// Status badge — the only place semantic color appears (DESIGN.md color rule 3).
import { badgeColorClass, type BadgeStatus } from './badge-status';

interface Props {
  status: BadgeStatus;
}

const { status } = Astro.props;
---

<span class={`badge ${badgeColorClass[status]}`}>{status}</span>
